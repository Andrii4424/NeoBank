export interface IBank{
  id: string | null,
  name: string | null,
  rating: number | null,
  hasLicense: boolean | null,
  bankFounderFullName: string | null,
  bankDirectorFullName: string | null,
  capitalization: number | null,
  employeesCount: number | null,
  blockedClientsCount: number | null,
  clientsCount: number | null,
  activeClientsCount: number | null,
  contactPhone: string | null,
  establishedDate: string | null,
  legalAddress: string | null,
  percentageCommissionForBuyingCurrency: number | null,
  percentageCommissionForSellingCurrency: number | null,
  swiftCode: string | null,
  mfoCode: string | null,
  taxId: string | null,
  contactEmail: string | null,
  updatedAt: string | null
}
